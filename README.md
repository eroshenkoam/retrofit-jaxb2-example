# Retrofit Jaxb2 example

## Usage

* `git clone https://github.com/eroshenkoam/retrofit-jaxb2-example.git` - checkout repository
* `mvn clean compile` - generate java classes and compile the project
